const esc = (value = '') => String(value)
  .replaceAll('&', '&amp;')
  .replaceAll('<', '&lt;')
  .replaceAll('>', '&gt;')
  .replaceAll('"', '&quot;');

const button = (label, disabled = false, className = '') => `<button class="btn ${className}"${disabled ? ' disabled aria-disabled="true"' : ''}>${esc(label)}</button>`;
const field = ({ label, value = '', placeholder = '', focused = false, disabled = false, type = 'text' }) => `
  <label class="field${focused ? ' focused' : ''}${disabled ? ' is-disabled' : ''}">
    <span>${esc(label)}</span>
    <input type="${esc(type)}" value="${esc(value)}" placeholder="${esc(placeholder)}"${disabled ? ' disabled' : ''} />
  </label>`;

function auth(x) {
  return `<section class="center-card auth-card">
    <div class="brand-dot"></div><h1>${esc(x.heading)}</h1><p class="muted">Use your account to continue</p>
    ${field({ label: 'Email', value: x.email, placeholder: 'you@example.com', focused: x.focus === 'email' })}
    ${field({ label: x.heading.includes('Verify') ? 'Verification code' : 'Password', value: x.password, placeholder: x.heading.includes('Verify') ? '6-digit code' : 'Enter password', focused: x.focus === 'password', type: 'text' })}
    ${x.status ? `<div class="signal ${x.status.includes('Incorrect') || x.status.includes('attempts') ? 'error' : 'info'}">${esc(x.status)}</div>` : ''}
    ${button(x.button, x.disabled, 'primary wide')}
    <a class="subtle-link">Need help?</a>
  </section>`;
}

function search(x) {
  const results = [
    ['Camera setup guide', 'A practical guide with examples'],
    ['Wireless keyboard collection', 'Compare features and availability'],
    ['Urban heat research notes', 'Peer-reviewed articles and datasets'],
  ];
  return `<section class="wide-card">
    <h1>${esc(x.heading)}</h1>
    ${field({ label: 'Search', value: x.query, placeholder: 'Search…', focused: true })}
    <div class="tabs"><span class="tab active">${esc(x.tab)}</span><span class="tab">Recent</span><span class="tab">Saved</span></div>
    <div class="search-meta"><strong>${esc(x.count)}</strong>${x.chip ? `<span class="chip">${esc(x.chip)}</span>` : ''}</div>
    ${x.notice ? `<div class="signal info">${esc(x.notice)}</div>` : ''}
    <div class="results">${results.map(([a, b], i) => `<article class="result"><span class="result-icon">${i + 1}</span><div><h3>${esc(a)}</h3><p>${esc(b)}</p></div></article>`).join('')}</div>
  </section>`;
}

function checkout(x) {
  return `<section class="checkout-grid">
    <div class="wide-card"><h1>${esc(x.heading)}</h1><div class="product-row"><div class="product-shape"></div><div><strong>${esc(x.item)}</strong><p class="muted">Quantity 1</p></div><strong class="price">${esc(x.total)}</strong></div>
      <div class="summary-line"><span>Subtotal</span><span>${esc(x.total)}</span></div><div class="summary-line"><span>Shipping</span><span>Included</span></div>
    </div>
    <aside class="side-card"><h2>Total</h2><div class="grand-total">${esc(x.total)}</div>${x.banner ? `<div class="signal ${x.banner.includes('declined') || x.banner.includes('changed') ? 'error' : 'warning'}">${esc(x.banner)}</div>` : ''}${button(x.button, x.disabled, 'primary wide')}</aside>
  </section>`;
}

function validation(x) {
  return `<section class="center-card form-card"><h1>${esc(x.heading)}</h1><p class="muted">Complete the required details.</p>
    <label class="field focused invalid"><span>${esc(x.label)}</span><input value="${esc(x.value)}" placeholder="${esc(x.placeholder)}" /></label>
    <div class="field-error">⚠ ${esc(x.error)}</div>
    ${field({ label: 'Notes', value: '', placeholder: 'Optional' })}
    ${button(x.button, true, 'primary wide')}
  </section>`;
}

function modal(x) {
  return `<section class="wide-card dimmed"><h1>${esc(x.page)}</h1><div class="fake-list">${['Overview', 'Recent activity', 'Shared with me', 'Archive'].map(t => `<div>${esc(t)}</div>`).join('')}</div></section>
    <div class="overlay"><div class="modal" role="dialog"><div class="danger-icon">!</div><h2>${esc(x.title)}</h2><p>${esc(x.body)}</p><div class="modal-actions">${button(x.secondary, false, 'secondary')}${button(x.primary, false, 'danger')}</div></div></div>`;
}

function toast(x) {
  return `<section class="wide-card"><h1>${esc(x.heading)}</h1><div class="settings-grid">${['General', 'Notifications', 'Privacy', 'Integrations'].map((t, i) => `<div class="setting-row"><span>${t}</span><span class="switch${i === 1 ? ' on' : ''}"></span></div>`).join('')}</div>${button(x.button, false, 'primary')}</section>
    <div class="toast ${esc(x.tone)}"><div class="toast-icon">${x.tone === 'success' ? '✓' : x.tone === 'error' ? '!' : x.tone === 'warning' ? '⚠' : 'i'}</div><div><strong>${esc(x.toast)}</strong>${x.detail ? `<small>${esc(x.detail)}</small>` : ''}</div></div>`;
}

function loading(x) {
  return `<section class="loading-page"><div class="spinner"></div><h1>${esc(x.heading)}</h1>${x.detail ? `<p>${esc(x.detail)}</p>` : ''}${x.progress ? `<div class="progress"><span style="width:${parseInt(x.progress, 10) || 50}%"></span></div><strong>${esc(x.progress)}</strong>` : ''}${!x.blank ? '<div class="skeleton-row"></div><div class="skeleton-row short"></div>' : ''}</section>`;
}

function consent(x) {
  return `<section class="article-page"><h1>Field Notes</h1><p>Independent stories about design, technology, and cities.</p><div class="hero-placeholder"></div><p class="article-lines">A long-form article continues underneath the privacy surface.</p></section>
    <div class="consent-banner"><div><h2>${esc(x.title)}</h2><p>${esc(x.body)}</p></div><div class="consent-actions">${x.buttons.map((b, i) => button(b, false, i === 0 ? 'primary' : 'secondary')).join('')}</div></div>`;
}

function dashboard(x) {
  return `<section class="dashboard"><div class="dashboard-head"><div><p class="eyebrow">Workspace analytics</p><h1>${esc(x.heading)}</h1></div><span class="chip">Live</span></div>${x.alert ? `<div class="signal ${x.alert.includes('degraded') || x.alert.includes('above') ? 'error' : 'warning'}">${esc(x.alert)}</div>` : ''}<div class="metric-grid">${x.metrics.map(([k, v], i) => `<article class="metric"><span>${esc(k)}</span><strong>${esc(v)}</strong><small>${i % 2 ? '↘ 2.4%' : '↗ 8.1%'}</small></article>`).join('')}</div><div class="spark-panel"><div class="spark-bars">${[36, 58, 42, 73, 64, 86, 71, 92].map(h => `<i style="height:${h}%"></i>`).join('')}</div></div></section>`;
}

function chart(x) {
  const max = Math.max(...x.values);
  return `<section class="chart-card"><div class="dashboard-head"><div><p class="eyebrow">Analytics</p><h1>${esc(x.heading)}</h1></div><span class="chip">Updated now</span></div><div class="bar-chart">${x.labels.map((label, i) => `<div class="bar-group"><div class="bar-value">${esc(String(x.values[i]))}${x.note && label === x.highlight ? `<em>${esc(x.note)}</em>` : ''}</div><div class="bar${label === x.highlight ? ' highlight' : ''}" style="height:${Math.round((x.values[i] / max) * 300)}px"></div><strong>${esc(label)}</strong></div>`).join('')}</div></section>`;
}

function table(x) {
  return `<section class="wide-card"><div class="dashboard-head"><h1>${esc(x.heading)}</h1>${button('Filter', false, 'secondary')}</div><table><thead><tr>${x.columns.map(c => `<th>${esc(c)}</th>`).join('')}</tr></thead><tbody>${x.rows.map((row, ri) => `<tr${ri === x.rows.length - 1 ? ' class="selected-row"' : ''}>${row.map((cell, ci) => `<td><span class="${ci === row.length - 1 ? 'status-pill' : ''}">${esc(cell)}</span></td>`).join('')}</tr>`).join('')}</tbody></table></section>`;
}

function email(x) {
  return `<section class="mail-shell"><aside class="mail-nav"><strong>Mail</strong>${button('Compose', false, 'primary wide')}<span>Inbox <b>12</b></span><span>Starred</span><span>Sent</span><span>Drafts <b>3</b></span></aside><main class="compose"><div class="compose-head"><h1>${esc(x.heading)}</h1><span>×</span></div>${field({ label: 'To', value: x.to, placeholder: 'Recipients' })}${field({ label: 'Subject', value: x.subject, placeholder: 'Subject' })}<label class="field body-field"><span>Message</span><textarea>${esc(x.body)}</textarea></label><div class="compose-foot">${button(x.button, false, 'primary')} ${x.status ? `<span class="mail-status">${esc(x.status)}</span>` : ''}</div></main></section>`;
}

function kanban(x) {
  return `<section class="board"><div class="dashboard-head"><h1>${esc(x.heading)}</h1><span class="chip">Board</span></div><div class="kanban">${x.columns.map(([name, cards]) => `<section class="kanban-col"><h2>${esc(name)} <span>${cards.length}</span></h2>${cards.map((card, i) => `<article class="kanban-card"><span class="tiny-tag">${i % 2 ? 'MEDIUM' : 'TASK'}</span><strong>${esc(card)}</strong><small>•••</small></article>`).join('')}</section>`).join('')}</div></section>`;
}

function calendar(x) {
  const days = Array.from({ length: 35 }, (_, i) => i - 2).map(n => n > 0 && n <= 31 ? String(n) : '');
  return `<section class="calendar-shell"><div class="dashboard-head"><h1>${esc(x.heading)}</h1><div>${button('Today', false, 'secondary')}</div></div><div class="weekday">${['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'].map(d => `<span>${d}</span>`).join('')}</div><div class="calendar-grid">${days.map(day => `<div class="day${day === x.selected ? ' selected-day' : ''}"><span>${day}</span>${day === x.selected ? `<article><strong>${esc(x.event)}</strong><small>${esc(x.time)}</small></article>` : ''}</div>`).join('')}</div>${x.conflict ? `<div class="signal warning">${esc(x.conflict)}</div>` : ''}</section>`;
}

function travel(x) {
  return `<section class="travel-shell"><aside class="travel-list"><h1>${esc(x.heading)}</h1><div class="selected-place"><strong>${esc(x.selected)}</strong><span>${esc(x.detail)}</span></div>${['Oak Corner', 'River Point', 'Central Stop'].map(v => `<div class="place">${esc(v)}<small>View details</small></div>`).join('')}</aside><div class="map">${Array.from({ length: x.pins }, (_, i) => `<span class="pin" style="left:${18 + (i * 13) % 66}%;top:${20 + (i * 19) % 58}%">${i + 1}</span>`).join('')}<div class="route-line"></div>${x.alert ? `<div class="map-alert">${esc(x.alert)}</div>` : ''}</div></section>`;
}

function gallery(x, assets) {
  const image1 = assets[x.asset] || '';
  const image2 = x.asset2 ? assets[x.asset2] : '';
  return `<section class="gallery-shell"><div class="dashboard-head"><div><p class="eyebrow">Open archive</p><h1>${esc(x.heading)}</h1></div><span class="chip">${esc(x.badge)}</span></div><div class="gallery-frame${image2 ? ' compare' : ''}"><figure><img src="${image1}" alt=""><figcaption>${esc(image2 ? 'Modern' : x.caption)}</figcaption></figure>${image2 ? `<figure><img src="${image2}" alt=""><figcaption>Historic</figcaption></figure>` : ''}</div><h2 class="gallery-caption">${esc(x.caption)}</h2></section>`;
}

function multilingual(x) {
  return `<section class="center-card form-card"${x.rtl ? ' dir="rtl"' : ''}><div class="language-pill">${x.rtl ? 'العربية' : 'A · 文'}</div><h1>${esc(x.heading)}</h1>${field({ label: x.label, value: x.value, placeholder: '—', focused: true })}${x.status ? `<div class="signal error">${esc(x.status)}</div>` : ''}${button(x.button, !!x.status, 'primary wide')}</section>`;
}

function occlusion(x) {
  return `<section class="wide-card occlusion-page"><h1>${esc(x.heading)}</h1><p class="muted">Update settings, then save your work.</p><div class="occlusion-form">${field({ label: 'Name', value: 'Example workspace', placeholder: '' })}${field({ label: 'Description', value: 'Production configuration', placeholder: '' })}</div><div class="covered-action">${button(x.target, false, 'primary')}<div class="cover-widget" style="opacity:${x.opacity}">${esc(x.cover)}</div></div></section>`;
}

function security(x) {
  return `<section class="center-card security-card"><div class="shield">✓</div><h1>${esc(x.heading)}</h1><p>Complete the verification below to continue.</p><div class="challenge"><span class="fake-checkbox"></span><strong>${esc(x.label)}</strong>${x.code ? `<div class="challenge-code">${esc(x.code)}</div>` : '<div class="challenge-grid"><i></i><i></i><i></i><i></i></div>'}</div>${x.status ? `<div class="signal warning">${esc(x.status)}</div>` : ''}${button(x.button, x.disabled, 'primary wide')}<small class="demo-label">Demo security challenge</small></section>`;
}

function unknown(x) {
  const needsBlur = !!x.obscured;
  return `<section class="document-shell"><div class="paper"><p class="eyebrow">Document viewer</p><h1>${esc(x.heading)}</h1><div class="document-line"><span>${esc(x.label)}</span><strong>${esc(x.visible)}</strong></div>${needsBlur ? `<div class="obscured-line"><span class="blur-block">K7Q-39X-A2</span><em>${esc(x.obscured)}</em></div>` : ''}<div class="doc-lines">${Array.from({ length: 7 }, (_, i) => `<i style="width:${90 - i * 7}%"></i>`).join('')}</div></div><aside class="viewer-tools"><strong>Page 1 of 1</strong><span>125%</span></aside></section>`;
}

const RENDERERS = { auth, search, checkout, validation, modal, toast, loading, consent, dashboard, chart, table, email, kanban, calendar, travel, gallery, multilingual, occlusion, security, unknown };

export function renderCaseHtml(caseDef, assets = {}) {
  const renderer = RENDERERS[caseDef.render.kind];
  if (!renderer) throw new Error(`Unknown render kind: ${caseDef.render.kind}`);
  const body = renderer(caseDef.render, assets);
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>${esc(caseDef.id)} — ${esc(caseDef.title)}</title><style>
  :root{--bg:#eef3f8;--card:#fff;--ink:#172033;--muted:#657086;--line:#dce3ec;--accent:#2563eb;--accent2:#dbeafe;--danger:#c92a2a;--warning:#b45309;--shadow:0 24px 70px rgba(28,39,60,.16)}
  html[data-theme="mint"]{--bg:#eaf6f1;--accent:#087f5b;--accent2:#d3f9d8}html[data-theme="amber"]{--bg:#fff7e8;--accent:#c2410c;--accent2:#ffedd5}html[data-theme="violet"]{--bg:#f3efff;--accent:#6d28d9;--accent2:#ede9fe}html[data-theme="slate"]{--bg:#dfe5ec;--ink:#101827;--accent:#0f172a;--accent2:#cbd5e1}
  *{box-sizing:border-box}body{margin:0;width:1280px;height:720px;overflow:hidden;background:var(--bg);color:var(--ink);font-family:Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;font-size:16px}body:before{content:"";position:fixed;inset:0;background:radial-gradient(circle at 8% 12%,rgba(255,255,255,.9),transparent 28%),radial-gradient(circle at 92% 90%,rgba(255,255,255,.7),transparent 25%);pointer-events:none}.topbar{height:62px;background:rgba(255,255,255,.88);border-bottom:1px solid var(--line);display:flex;align-items:center;padding:0 34px;gap:34px;position:relative;z-index:2}.logo{font-weight:850;letter-spacing:-.04em}.logo b{color:var(--accent)}.topbar nav{display:flex;gap:26px;color:var(--muted);font-size:14px}.topbar .avatar{margin-left:auto;width:32px;height:32px;border-radius:50%;background:linear-gradient(135deg,var(--accent),#22c55e);box-shadow:inset 0 0 0 4px white}.viewport{height:658px;padding:36px 50px;position:relative}.center-card,.wide-card,.side-card,.chart-card,.calendar-shell,.gallery-shell,.document-shell,.dashboard,.board{background:var(--card);border:1px solid rgba(150,165,185,.25);border-radius:20px;box-shadow:var(--shadow)}.center-card{width:440px;margin:0 auto;padding:34px 42px}.wide-card{max-width:1060px;margin:0 auto;padding:32px 38px}h1,h2,h3,p{margin-top:0}h1{font-size:30px;letter-spacing:-.035em;margin-bottom:10px}h2{letter-spacing:-.02em}.muted{color:var(--muted)}.eyebrow{text-transform:uppercase;font-size:12px;letter-spacing:.12em;color:var(--muted);font-weight:800;margin-bottom:6px}.brand-dot{width:36px;height:36px;border-radius:12px;background:linear-gradient(135deg,var(--accent),#22c55e);margin-bottom:20px}.field{display:block;margin:18px 0;text-align:left}.field>span{display:block;font-weight:720;font-size:13px;margin-bottom:7px;color:#344055}.field input,.field textarea{width:100%;border:1px solid #cbd5df;border-radius:11px;background:white;padding:13px 14px;font:inherit;color:var(--ink);outline:none}.field textarea{height:190px;resize:none}.field.focused input{border:2px solid var(--accent);box-shadow:0 0 0 4px color-mix(in srgb,var(--accent) 14%,transparent)}.field.invalid input{border:2px solid #e03131;background:#fff5f5}.field.is-disabled{opacity:.55}.field-error{color:#c92a2a;font-size:13px;margin-top:-10px;margin-bottom:18px;font-weight:700}.btn{border:0;border-radius:10px;padding:12px 18px;background:#e8edf3;color:#273247;font-weight:760;font-size:15px}.btn.primary{background:var(--accent);color:white}.btn.danger{background:#d92d20;color:white}.btn.secondary{background:white;border:1px solid #cad2de}.btn.wide{width:100%}.btn.disabled{opacity:.42;filter:saturate(.45)}.subtle-link{display:block;text-align:center;margin-top:17px;color:var(--accent);font-size:13px}.signal{padding:12px 14px;border-radius:10px;margin:16px 0;font-weight:680;font-size:14px}.signal.error{background:#fff0f0;color:#b42318;border:1px solid #f7b8b8}.signal.warning{background:#fff8e1;color:#8a4b00;border:1px solid #f3d18b}.signal.success{background:#e8f8ee;color:#137333}.signal.info{background:#eef6ff;color:#185abc}.tabs{display:flex;gap:8px;margin:18px 0}.tab{padding:8px 13px;border-radius:9px;color:var(--muted);font-weight:700}.tab.active{background:var(--accent2);color:var(--accent)}.search-meta{display:flex;align-items:center;gap:14px;border-bottom:1px solid var(--line);padding-bottom:16px}.chip{padding:7px 10px;background:var(--accent2);color:var(--accent);border-radius:99px;font-size:12px;font-weight:800}.results{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:18px}.result{border:1px solid var(--line);border-radius:13px;padding:14px;display:flex;gap:12px}.result h3{font-size:15px;margin-bottom:4px}.result p{font-size:13px;color:var(--muted);margin:0}.result-icon{width:30px;height:30px;border-radius:9px;background:var(--accent2);color:var(--accent);display:grid;place-items:center;font-weight:800}.checkout-grid{display:grid;grid-template-columns:1.6fr .8fr;gap:24px;max-width:1100px;margin:0 auto}.checkout-grid .wide-card{width:100%}.side-card{padding:30px;height:max-content}.product-row{display:flex;align-items:center;gap:18px;border:1px solid var(--line);padding:18px;border-radius:14px;margin:22px 0}.product-shape{width:78px;height:78px;border-radius:18px;background:linear-gradient(145deg,var(--accent2),var(--accent))}.price{margin-left:auto}.summary-line{display:flex;justify-content:space-between;padding:12px 0;border-bottom:1px solid var(--line)}.grand-total{font-size:38px;font-weight:860;letter-spacing:-.04em;margin:18px 0}.fake-list{display:grid;gap:12px;margin-top:22px}.fake-list div{height:54px;border:1px solid var(--line);border-radius:11px;padding:16px}.dimmed{filter:blur(1px)}.overlay{position:absolute;inset:0;background:rgba(15,23,42,.58);display:grid;place-items:center;z-index:5}.modal{width:480px;background:white;padding:34px;border-radius:20px;box-shadow:0 34px 90px rgba(0,0,0,.38)}.danger-icon{width:42px;height:42px;border-radius:50%;background:#fee2e2;color:#b91c1c;display:grid;place-items:center;font-size:24px;font-weight:900;margin-bottom:16px}.modal-actions{display:flex;justify-content:flex-end;gap:10px;margin-top:26px}.settings-grid{margin:24px 0}.setting-row{display:flex;justify-content:space-between;padding:18px 0;border-bottom:1px solid var(--line)}.switch{width:42px;height:24px;background:#cbd5e1;border-radius:99px;position:relative}.switch:after{content:"";position:absolute;width:18px;height:18px;left:3px;top:3px;border-radius:50%;background:white}.switch.on{background:var(--accent)}.switch.on:after{left:21px}.toast{position:absolute;right:56px;top:35px;min-width:310px;background:#172033;color:white;border-radius:14px;padding:15px 18px;box-shadow:0 18px 50px rgba(0,0,0,.28);display:flex;gap:12px;z-index:4}.toast.error{background:#8f1d1d}.toast.warning{background:#7a4300}.toast.info{background:#164e63}.toast-icon{width:28px;height:28px;border-radius:50%;display:grid;place-items:center;background:rgba(255,255,255,.2);font-weight:900}.toast small{display:block;opacity:.78;margin-top:4px}.loading-page{height:100%;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center}.loading-page h1{font-size:25px}.spinner{width:50px;height:50px;border:5px solid #d6dee9;border-top-color:var(--accent);border-radius:50%;margin-bottom:22px}.progress{width:360px;height:10px;background:#dce3ec;border-radius:99px;overflow:hidden;margin:16px}.progress span{display:block;height:100%;background:var(--accent)}.skeleton-row{width:500px;height:20px;background:linear-gradient(90deg,#dce3ec,#f8fafc,#dce3ec);border-radius:8px;margin-top:22px}.skeleton-row.short{width:350px;margin-top:10px}.article-page{max-width:900px;margin:0 auto;background:white;border-radius:18px;padding:32px}.hero-placeholder{height:230px;border-radius:14px;background:linear-gradient(135deg,var(--accent2),#d1d5db)}.article-lines{color:var(--muted);margin-top:20px}.consent-banner{position:absolute;left:38px;right:38px;bottom:28px;background:#111827;color:white;border-radius:18px;padding:22px 26px;display:flex;align-items:center;justify-content:space-between;gap:30px;box-shadow:0 28px 80px rgba(0,0,0,.35)}.consent-banner h2{margin-bottom:6px}.consent-banner p{margin:0;color:#cbd5e1}.consent-actions{display:flex;gap:10px;min-width:max-content}.dashboard,.board,.chart-card,.calendar-shell,.gallery-shell{max-width:1120px;margin:0 auto;padding:30px}.dashboard-head{display:flex;justify-content:space-between;align-items:center}.metric-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin:24px 0}.metric{border:1px solid var(--line);border-radius:16px;padding:20px}.metric span,.metric small{display:block;color:var(--muted)}.metric strong{font-size:34px;display:block;margin:10px 0}.metric small{color:#16803d}.spark-panel{height:190px;border:1px solid var(--line);border-radius:16px;padding:20px}.spark-bars{height:100%;display:flex;align-items:flex-end;gap:12px}.spark-bars i{flex:1;background:linear-gradient(var(--accent),var(--accent2));border-radius:7px 7px 0 0}.bar-chart{height:430px;display:flex;align-items:flex-end;justify-content:center;gap:34px;border-left:1px solid var(--line);border-bottom:1px solid var(--line);padding:30px 30px 0;margin-top:24px}.bar-group{width:120px;text-align:center;position:relative}.bar{width:78px;margin:0 auto 12px;background:linear-gradient(180deg,var(--accent),color-mix(in srgb,var(--accent) 55%,white));border-radius:10px 10px 2px 2px}.bar.highlight{box-shadow:0 0 0 5px var(--accent2)}.bar-value{font-weight:850;margin-bottom:8px}.bar-value em{position:absolute;left:50%;transform:translateX(-50%);top:-34px;width:max-content;font-style:normal;font-size:12px;background:#172033;color:white;padding:6px 8px;border-radius:7px}table{width:100%;border-collapse:collapse;margin-top:22px}th,td{text-align:left;padding:17px 14px;border-bottom:1px solid var(--line)}th{color:var(--muted);font-size:12px;text-transform:uppercase;letter-spacing:.08em}.selected-row{background:var(--accent2)}.status-pill{padding:6px 9px;border-radius:99px;background:#eef2f7;font-weight:750;font-size:13px}.mail-shell{height:100%;display:grid;grid-template-columns:220px 1fr;background:white;border-radius:18px;overflow:hidden;box-shadow:var(--shadow)}.mail-nav{background:#f8fafc;border-right:1px solid var(--line);padding:28px 20px;display:flex;flex-direction:column;gap:18px}.mail-nav>strong{font-size:25px;margin-bottom:12px}.mail-nav>span{color:#4b5563}.mail-nav b{float:right}.compose{padding:26px 34px}.compose-head{display:flex;justify-content:space-between}.body-field{margin-bottom:8px}.compose-foot{display:flex;align-items:center;gap:18px}.mail-status{color:var(--warning);font-weight:750}.kanban{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:22px}.kanban-col{background:#f6f8fb;border:1px solid var(--line);border-radius:15px;padding:15px;min-height:390px}.kanban-col h2{font-size:16px;display:flex;justify-content:space-between}.kanban-col h2 span{color:var(--muted)}.kanban-card{background:white;border:1px solid var(--line);border-radius:12px;padding:16px;margin:12px 0;box-shadow:0 6px 20px rgba(30,45,70,.06)}.kanban-card strong,.kanban-card small{display:block}.kanban-card small{text-align:right;color:var(--muted)}.tiny-tag{font-size:10px;font-weight:850;color:var(--accent);letter-spacing:.08em}.weekday,.calendar-grid{display:grid;grid-template-columns:repeat(7,1fr)}.weekday span{text-align:center;color:var(--muted);font-size:12px;font-weight:800;padding:16px}.day{height:78px;border:1px solid var(--line);padding:7px;font-size:12px}.selected-day{background:var(--accent2);box-shadow:inset 0 0 0 2px var(--accent)}.selected-day article{background:var(--accent);color:white;border-radius:6px;padding:5px;margin-top:4px}.selected-day article strong,.selected-day article small{display:block;font-size:9px}.travel-shell{height:100%;display:grid;grid-template-columns:340px 1fr;background:white;border-radius:18px;overflow:hidden;box-shadow:var(--shadow)}.travel-list{padding:26px;border-right:1px solid var(--line)}.selected-place,.place{padding:16px;border:1px solid var(--line);border-radius:12px;margin:12px 0}.selected-place{border:2px solid var(--accent);background:var(--accent2)}.selected-place span,.place small{display:block;color:var(--muted);margin-top:5px}.map{position:relative;background-color:#dce7db;background-image:linear-gradient(32deg,transparent 46%,rgba(255,255,255,.75) 47%,rgba(255,255,255,.75) 53%,transparent 54%),linear-gradient(118deg,transparent 46%,rgba(255,255,255,.6) 47%,rgba(255,255,255,.6) 53%,transparent 54%);background-size:130px 110px}.pin{position:absolute;width:34px;height:34px;border-radius:50% 50% 50% 6px;background:#e11d48;color:white;display:grid;place-items:center;transform:rotate(-45deg);font-weight:850;box-shadow:0 6px 15px rgba(0,0,0,.22)}.pin::first-letter{transform:rotate(45deg)}.route-line{position:absolute;left:20%;top:54%;width:58%;height:5px;background:var(--accent);transform:rotate(-12deg);border-radius:99px}.map-alert{position:absolute;right:20px;top:20px;background:#7f1d1d;color:white;border-radius:10px;padding:13px 16px;font-weight:800}.gallery-frame{height:400px;background:#111827;border-radius:16px;display:grid;place-items:center;overflow:hidden;margin-top:22px}.gallery-frame.compare{grid-template-columns:1fr 1fr;gap:3px}.gallery-frame figure{margin:0;width:100%;height:100%;position:relative}.gallery-frame img{width:100%;height:100%;object-fit:cover;image-rendering:auto}.gallery-frame figcaption{position:absolute;left:16px;bottom:14px;background:rgba(0,0,0,.7);color:white;padding:7px 10px;border-radius:8px}.gallery-caption{text-align:center;margin:16px 0 0}.language-pill{float:right;background:var(--accent2);color:var(--accent);padding:7px 10px;border-radius:99px;font-weight:850}.occlusion-form{max-width:620px}.covered-action{position:relative;width:220px;margin-top:26px}.cover-widget{position:absolute;left:72px;top:-9px;background:#111827;color:white;border-radius:14px;padding:16px 23px;min-width:170px;text-align:center;box-shadow:0 16px 40px rgba(0,0,0,.3);z-index:2}.security-card{text-align:center}.shield{width:48px;height:48px;margin:0 auto 14px;background:var(--accent2);color:var(--accent);border-radius:50%;display:grid;place-items:center;font-size:24px}.challenge{border:1px solid var(--line);border-radius:13px;padding:16px;display:flex;align-items:center;gap:12px;text-align:left;margin:20px 0}.fake-checkbox{width:25px;height:25px;border:2px solid #9aa5b1;border-radius:4px}.challenge-grid{margin-left:auto;display:grid;grid-template-columns:repeat(2,22px);gap:3px}.challenge-grid i{height:22px;background:linear-gradient(135deg,#94a3b8,#334155)}.challenge-code{margin-left:auto;font:800 24px monospace;letter-spacing:4px;filter:blur(.6px)}.demo-label{display:block;color:var(--muted);margin-top:13px}.document-shell{max-width:980px;height:100%;margin:0 auto;display:grid;grid-template-columns:1fr 180px;overflow:hidden}.paper{padding:50px 62px}.document-line{display:flex;justify-content:space-between;border-top:1px solid var(--line);border-bottom:1px solid var(--line);padding:22px 0;margin-top:28px}.document-line strong{font:800 22px ui-monospace,monospace}.obscured-line{display:flex;align-items:center;gap:20px;margin:22px 0;padding:14px;background:#fff8e1;border-radius:9px}.blur-block{filter:blur(6px);font:800 18px monospace}.obscured-line em{color:var(--warning)}.doc-lines{margin-top:38px}.doc-lines i{display:block;height:8px;background:#dce3ec;margin:13px 0;border-radius:99px}.viewer-tools{border-left:1px solid var(--line);background:#f8fafc;padding:28px;display:flex;flex-direction:column;gap:18px;color:var(--muted)}
  </style></head><body><script>document.documentElement.dataset.theme=${JSON.stringify(caseDef.render.theme)}</script><header class="topbar"><div class="logo">Web<b>Scene</b></div><nav><span>Home</span><span>Workspace</span><span>Activity</span></nav><div class="avatar"></div></header><main class="viewport">${body}</main></body></html>`;
}
