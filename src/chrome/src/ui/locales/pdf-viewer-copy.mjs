// PDF viewer settings copy. Keep this small feature-specific surface separate
// from the generated locale dictionaries so missing translations fall back to
// English without making every locale file part of the implementation change.
export const pdfViewerEnglish = {
  'st.display.pdf_viewer.label': 'Use WebBrain PDF viewer',
  'st.display.pdf_viewer.desc': 'On Chrome 151+, replace Chrome’s native viewer for online PDFs with a selectable WebBrain PDF.js viewer. Off keeps the native viewer; the explicit context-menu entry remains available.',
};
